## E-mail sent with the information filled out in a feedback form.
##
## Parameters: {0} current date
##             {1} email address that the user provided
##             {2} logged in as
##             {3} page that the user was on when they selected feedback
##             {4} User-Agent HTTP Header
##             {5} Session Id
##             {6} The user's comments
##
## See org.dspace.core.Email for information on the format of this file.
##
#set($mimetype = "text/html")
#set($subject = "${config.get('dspace.name')}: Feedback Form Information")
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Feedback Received</title>
    <style>
        #parse("_email_common_styles")
        .comments {
            background-color: #f9f9f9;
            padding: 20px;
            border-radius: 4px;
            margin: 20px 0;
            border-left: 4px solid #007cba;
        }
        .details {
            background-color: #f0f7ff;
            padding: 15px;
            border-radius: 4px;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>User Feedback</h2>

        <div class="comments">
            <strong>Comments:</strong>
            <p>${params[6]}</p>
        </div>

        <div class="details">
            <h3>Submission Details</h3>
            <div class="detail-item"><strong>Date:</strong> ${params[0]}</div>
            <div class="detail-item"><strong>Email:</strong> ${params[1]}</div>
            <div class="detail-item"><strong>Logged In As:</strong> ${params[2]}</div>
            <div class="detail-item"><strong>Referring Page:</strong> ${params[3]}</div>
            <div class="detail-item"><strong>User Agent:</strong> ${params[4]}</div>
            <div class="detail-item"><strong>Session:</strong> ${params[5]}</div>
        </div>

        <p class="signature">The ${config.get('dspace.name')} Team</p>
    </div>
</body>
</html>