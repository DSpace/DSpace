## E-mail enviado aos usuários do InterAcoes ao se registrarem

#set($subject = "${config.get('dspace.name')} Cadastro de Conta" )
#set($phone = ${config.get('mail.message.helpdesk.telephone')})


Para concluir o cadastro de uma conta no ${config.get('dspace.name')}, clique no link abaixo:

  ${params[0]}

##Se precisar de ajuda com sua conta, envie um e-mail para ${config.get("mail.helpdesk")}.

#if( $phone )
Ou entre em contato pelo telefone: ${phone}
#end

Equipe ${config.get('dspace.name')}
