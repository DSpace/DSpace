dspace_host:
  # dspace api base (no trailing slash)
  "http://localhost:8080/server"
solr_url:
  # solr host (no trailing slash)
  "http://localhost:8983/solr"
solr_core:
  # the solr core name
  "word_highlighting"
file_format:
  # alto or miniocr
  "miniocr"
index_type:
  # lazy or full
  "lazy"
xml_file_location:
  # file path used for lazy index
  "/Users/mspalti/escaped_alto"
http_port:
  # port used by this service
  "3000"
log_dir:
  "/Users/mspalti/logs"
