--- a/dspace-jspui/src/main/java/org/dspace/app/webui/servlet/HandleServlet.java
+++ b/dspace-jspui/src/main/java/org/dspace/app/webui/servlet/HandleServlet.java
@@ -399,6 +399,7 @@ public class HandleServlet extends DSpaceServlet
             }
         }
 
+        try {
         // Fire usage event.
         new DSpace().getEventService().fireEvent(
             		new UsageEvent(
@@ -406,7 +407,9 @@ public class HandleServlet extends DSpaceServlet
             				request,
             				context,
             				item));
-
+        } catch (Exception e) {
+            log.warn("could not record usage event", e);
+        }
         // Set attributes and display
         request.setAttribute("suggest.enable", Boolean.valueOf(suggestEnable));
         request.setAttribute("display.all", Boolean.valueOf(displayAll));
